# 贡献指南 · Contributing

感谢你愿意为 **班迪 AI 技能库** 添砖加瓦!本仓库是开源项目,欢迎提交技能、改进文档、报告问题。

## 一、如何提交一个技能

1. **Fork 本仓库**,在 `skills/` 下新建技能目录,遵循统一结构:

```
skills/<技能名>/
├── SKILL.md                  ← 主指令(必填)
├── references/               ← 模板库 / 参考(可选)
│   └── *.md
└── scripts/                  ← 脚本增强(可选)
    └── *.py
```

2. **SKILL.md 规范**:
   - 开头写明技能名称、用途、触发条件;
   - 流程分阶段,每阶段有明确产出与确认点;
   - 尽量**纯文本指令**,不绑定具体平台/API(可移植性优先);
   - 涉及提示词模板的统一放 `references/`。

3. 在 `README.md` 的技能列表表格中登记你的技能。

## 二、代码与文档规范

- 中文注释,语言风格与现有文档保持一致;
- Python 脚本遵守 PEP 8,标注运行依赖(尽量零依赖);
- 不引入任何闭源/付费依赖,保持"纯本地可运行"。

## 三、提交 PR

1. 描述清楚:做了什么、为什么、如何验证;
2. 保持提交粒度清晰(一个 PR 一个主题);
3. 合并前需通过简单自检:技能流程可通读、脚本可运行。

## 四、行为准则

- 友善、专业,对事不对人;
- 不提交侵权内容(含 AI 生成物的版权风险请自审);
- 商业合作意向请走授权渠道(见 README"联系我"),不要在 Issue 里谈商务。

## 五、协议

贡献即视为同意你的贡献以本仓库 **班迪多技能开源协议 v1.0** 授权发布。
